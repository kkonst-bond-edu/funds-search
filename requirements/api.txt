# API service requirements (lightweight, no ML libraries)
# Used by: apps/api (orchestrator only)

# Include base requirements
-r base.txt

# LangChain and LangGraph for orchestration
langchain>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0
langgraph>=0.1.0
tenacity>=8.2.0

# Firecrawl for real vacancy search
firecrawl-py>=1.0.0

