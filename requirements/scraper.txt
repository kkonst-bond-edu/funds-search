# Scraper-specific requirements (lightweight, no ML libraries)
# Used by: src/services/scraper (GitHub Actions workflow)

# Core dependencies
python-dotenv>=1.0.0
pydantic>=2.0.0
python-multipart>=0.0.7

# Web scraping
playwright>=1.41.0
httpx>=0.27.0
beautifulsoup4>=4.12.0

# Logging
structlog>=24.1.0

# Pinecone for vector storage
pinecone
numpy>=1.26.0

# LangChain for AI classification (lightweight, no torch)
langchain>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0
langchain-core>=0.2.0
tenacity>=8.2.0

# Configuration
pyyaml>=6.0.0

# Azure Blob Storage for HTML archiving
azure-storage-blob>=12.19.0
azure-identity>=1.15.0
